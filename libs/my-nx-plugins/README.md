# my-nx-plugins

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-nx-plugins` to build the library.

## Running unit tests

Run `nx test my-nx-plugins` to execute the unit tests via [Jest](https://jestjs.io).
